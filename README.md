# nginv
A real-time terminal dashboard for monitoring multiple nginx log files. Similar to `top` or `bmon`, but for your -  provides a live, auto-refreshing view of your nginx server traffic across multiple sites nginx traffic.
